# Copy to .env.staging on the staging server.

# Public app URL. Keep http while the staging domain is not HTTPS-enabled.
SKYBLOOM_PUBLIC_BASE_URL=http://your-staging-domain
SKYBLOOM_PUBLIC_WS_BASE_URL=ws://your-staging-domain

# Browser-facing frontend settings.
NEXT_PUBLIC_SUPABASE_URL=https://project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=replace-me
NEXT_PUBLIC_SUPABASE_REDIRECT_URL=http://your-staging-domain/auth/callback
VITE_API_PROXY_TARGET=http://reverse-proxy
VITE_ALLOWED_HOSTS=your-staging-domain

# Supabase Storage S3-compatible credentials.
AWS_ACCESS_KEY_ID=replace-me
AWS_SECRET_ACCESS_KEY=replace-me
AWS_REGION=ap-southeast-2
AWS_S3_ENDPOINT_URL=https://project-ref.storage.supabase.co/storage/v1/s3
AWS_S3_BUCKET=documents
AWS_S3_PREFIX=staging

# Postgres document indexing.
POSTGRES_HOST=replace-me
POSTGRES_PORT=5432
POSTGRES_DB=postgres
POSTGRES_USER=replace-me
POSTGRES_PASSWORD=replace-me
POSTGRES_SSLMODE=require

# OpenAI settings for document sectioning and game generation.
OPENAI_API_KEY=replace-me
OPENAI_BASE_URL=https://api.openai.com/v1
SECTIONING_LLM_MODEL=gpt-4o-mini
SECTIONING_LLM_TIMEOUT_SECONDS=120
SECTIONING_LLM_MAX_RETRIES=2
SECTIONING_OUTLINE_MAX_REPAIRS=2
LEVEL_LLM_MODEL=gpt-4o-mini
LEVEL_LLM_TEMPERATURE=0.2
LEVEL_LLM_TIMEOUT_SECONDS=60
LEVEL_LLM_MAX_RETRIES=3
LEVEL_SOURCE_MAX_CHARS=24000

# User service and auth cookie.
USER_SERVICE_PORT=8080
SUPABASE_URL=https://project-ref.supabase.co
SUPABASE_JWT_SECRET=replace-me
SUPABASE_JWT_AUDIENCE=authenticated
SUPABASE_JWKS_URL=
ALLOW_UNVERIFIED_JWT=false
AUTH_ACCESS_TOKEN_COOKIE=skybloom_access_token
AUTH_COOKIE_SECURE=false
CORS_ALLOWED_ORIGINS=*
